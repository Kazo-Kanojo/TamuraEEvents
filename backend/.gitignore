# Dependências (Pesadas, não devem subir)
node_modules/

# Arquivos de Ambiente (SEGURANÇA MÁXIMA)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Builds de produção (Gerados automaticamente)
dist/
build/

# Logs de erro
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Arquivos de sistema (Mac/Windows)
.DS_Store
Thumbs.db

# Banco de dados (Se quiser manter local e não subir o arquivo de dados)
database.sqlite
# Mas se quiser subir um banco vazio/inicial, pode remover essa linha acima